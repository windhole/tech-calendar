## 23:37 〜 CSV 列名を tag から 開催地 へ

### やったこと
- csv2yaml が読む列を `開催地` にした。古い `tag` 列もまだ読む
- README / ADR-0019 / ADR-0006 の列名を合わせた

### 関連ADR / コミット
- ADR-0019

## 23:51 〜 リスト表示の今日以降に件数を出す

### やったこと
- `EventList` に `showCount` を足し、リスト表示の「今日以降のイベント」の横に `N件` の Badge を出した
- 「過去のイベント」とカレンダー下の一覧では出さない


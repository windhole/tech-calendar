## 10:14 〜 アクセス計測ツールの比較メモ

### やったこと
- GoatCounter と Cloudflare Web Analytics を公式ドキュメントから調べ、`docs/analytics-goatcounter-vs-cloudflare.md` に比較を書いた
- Cloudflare Access は別製品であること、GitHub Pages にサイトログがないことも書いた

### 関連ADR / コミット
- (ADRなし。導入前の調査)

## 10:28 〜 GoatCounter を入れる

### やったこと
- まずは GoatCounter、利用が増えたら Cloudflare Web Analytics と Pages 移行を検討する、と決めた
- ADR-0009 を書いた
- 本番かつ `VITE_GOATCOUNTER_COUNT_URL` があるときだけ `count.js` を読み、React Router の遷移で `goatcounter.count` する
- localhost では読まない。GitHub Actions は `vars.VITE_GOATCOUNTER_COUNT_URL` をビルドに渡す

### 関連ADR / コミット
- ADR-0009

## 10:49 〜 GitHub 変数の入れ方を README に書いた

### やったこと
- Actions の Variables であること、Agents や Secrets や `github-pages` environment ではないことを README に具体的に書いた

## 10:57 〜 csv2yaml の出力名をシリアル番号にする

### やったこと
- 出力を `data/events_YYYYMMDD_NNN.yaml` に変更した（時刻ではなく、その日の 3 桁シリアル）
- 同じ日の既存ファイルの最大番号の次を使い、古い `YYYYMMDD-HHMM` 名は数えない
- ADR-0010 を書き、ADR-0006 のファイル名だけをそちらへ寄せた

### 関連ADR / コミット
- ADR-0010

## 13:32 〜 events.yaml の確認スクリプト

### やったこと
- `check_events.rb` を追加。開催期間が重なるイベントが 3 件以上の日付を表示する
- 既定の入力は `public/events.yaml`。`make check-events` でも実行できる

### 関連ADR / コミット
- ADR-0011

## 13:48 〜 日付逆転とイベント名重複は csv2yaml のエラーにする

### やったこと
- `endDate` が `startDate` より前、またはイベント名が重複しているときは `csv2yaml.rb` が YAML を書かずに終了する
- `check_events.rb` からはその 2 つの点検を外し、1 日 3 件以上だけにした

### 関連ADR / コミット
- ADR-0012

## 14:14 〜 ヘッダの名称と「今日」の区別、YAML 更新日時

### やったこと
- 名称を techカレンダーにした（タブタイトル、左上、フッタ）
- 右上を「カレンダー表示」「リスト表示」の切り替えにし、「今日」は分けて青い丸ボタンにした
- タイトル下に、読み込んだ events.yaml の Last-Modified を小さく出す

### 関連ADR / コミット
- ADR-0013

## 14:44 〜 「今日」を表示切替の左へ

### やったこと
- 右上の並びを「今日」「カレンダー表示」「リスト表示」にした
- 表示切替を右端に固定して、リスト表示で「今日」が消えても位置がずれないようにした

## 14:55 〜 ダイアログのリンク文言

### やったこと
- イベント詳細ダイアログのリンクを「詳細を開く」から「イベントサイトを開く」に変えた

## 15:06 〜 複数の events*.yaml を読む

### やったこと
- `public/events*.yaml` をすべて読み、同じ開始日・同じ名前は新しいファイルの内容を残すようにした
- GitHub Pages ではディレクトリを列挙できないので、ビルド時にファイル名を拾う
- `check_events.rb` も同じ合成ルールにした

### 関連ADR / コミット
- ADR-0014

## 15:55 〜 README を現状に合わせる

### やったこと
- 「作り直している段階」をやめ、GitHub Pages で公開中であることにした
- ヘッダの表示切替と「今日」、セル最大 5 件、ホバーとダイアログ、YAML 更新日時を機能に書いた
- csv2yaml / check_events、複数 YAML の合成、GoatCounter、ディレクトリ構成を足した

### 関連ADR / コミット
- (ADRなし。ドキュメントのみ)


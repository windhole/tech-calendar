## 07:30 〜 カレンダーのイベントにツールチップと詳細ダイアログ

### やったこと
- 月間カレンダーのイベント名にホバー／フォーカスで全文ツールチップを付けた
- クリックで開催日・会場・URL の Dialog を出す。URL は別タブで開く
- チップはボタンにし、見た目は `monthly-calendar.css` のままにした

### 関連ADR / コミット
- ADR-0007

## 08:47 〜 Bolt / 未使用 Supabase の削除

### やったこと
- `.bolt/` を削除した
- `index.html` の bolt.new 向け OG / Twitter 画像メタを外した
- `package.json` の名前を `tech-calendar` にし、未使用の `@supabase/supabase-js` を外した

### 関連ADR / コミット
- (ADRなし。テンプレ残骸の削除)

## 21:37 〜 「今日」で YAML を再取得する

### やったこと
- 「今日」クリックで `events.yaml` と表示中の年の祝日 YAML を取り直す
- 再取得時は `cache: 'no-store'` と `?t=` でキャッシュを避ける。画面全体の読み込み中には戻さない

### 関連ADR / コミット
- ADR-0008

## 22:06 〜 セル内イベントを最大5件まで出す

### やったこと
- 日付セルの表示上限を 2 件から 5 件にした。6 件以上だけ `+N件`
- セル高さは中身に合わせ、同じ週は一番高い日に揃える

### 関連ADR / コミット
- (ADRなし。表示件数とセル高さの調整)

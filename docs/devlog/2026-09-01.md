## 22:00 〜 月曜始まりの月間カレンダーと GitHub Pages

### やったこと
- 月間カレンダーを `src/calendar/` に切り出し、対象月を含む 6 週間・月曜始まりにした（2026年8月は 7/27〜9/6）
- 祝日を `data/holidays.yaml` に移し、ビルド時に `js-yaml` で読み込む
- 見た目は `monthly-calendar.css` の CSS 変数と BEM 風クラスに集約（土=薄青、日・祝=薄赤）
- GitHub Actions で `dist/` を GitHub Pages に載せる workflow を追加。Vite `base` は `/tech-calendar/`

### 詰まった点・気づき
- `js-yaml` 5 は default export がなく、`load` の named import が必要だった
- 既存の ESLint エラーは shadcn の `src/components/ui/` 側で、今回の変更とは無関係

### 関連ADR / コミット
- ADR-0001

## 22:18 〜 ブラウザと CI の確認

### やったこと
- ローカル `http://127.0.0.1:5173/tech-calendar/` で 2026年8月を表示し、7/27〜9/6・土曜薄青・日曜祝日薄赤・8/11 山の日を確認
- 390x844 のモバイル幅でも横スクロールなく表示できることを確認
- PR #1 の GitHub Actions `build` が pass

### 関連ADR / コミット
- ADR-0001 / 4b9a860 / https://github.com/windhole/tech-calendar/pull/1

## 22:39 〜 パッケージマネージャを bun に切り替え

### やったこと
- ADR-0002 を追加し、`package-lock.json` を `bun.lock` に置き換えた
- CI を `oven-sh/setup-bun`（bun 1.4.0）+ `bun install --frozen-lockfile` に変更
- README の手順を `bun install` / `bun run …` に更新
- `bun run typecheck` と `bun run build` が通ることを確認

### 関連ADR / コミット
- ADR-0002

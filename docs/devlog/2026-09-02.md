## 07:24 〜 期間内イベントと全件ページ

### やったこと
- カレンダー下の一覧を、表示中 6 週間と重なるイベントだけにした
- `/events` を追加。先頭は今日以降（開始日昇順）、その下に過去（開始日降順）
- `react-router-dom` でページを分け、Pages 用に `dist/404.html` をコピーする
- 確認しやすいよう 9月・10月のサンプルイベントを 2 件足した

### 関連ADR / コミット
- ADR-0003

## 12:30 〜 祝日を年別で public に移す

### やったこと
- 祝日を `public/holidays/YYYY.yaml` に分け、実行時 fetch に変更した
- 表示中の 6 週間がまたぐ年だけ読む。無い年は空
- `data/holidays.yaml` を削除した

### 関連ADR / コミット
- ADR-0004

## 15:00 〜 イベントを YAML 1 ファイルへ

### やったこと
- イベントを `public/events.yaml` に移し、`js-yaml` で読むようにした
- `events.jsonl` を削除した

### 関連ADR / コミット
- ADR-0005

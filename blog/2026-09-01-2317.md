# 日付は選ばず、一覧は下へ

日付セルをクリックしてその日のイベントだけ出す、という動きをやめた。選んでいないのに「選択をクリア」が出るのも、出ないのも、どちらも要らない。ヘッダには「今日」だけ残した。

イベントはカレンダーの横ではなく、月間カレンダーの直下に「すべてのイベント」として出す。画面幅が広いときも縦に積む。セルはもうボタンではないので、今日の枠以外に選択リングは付かない。

確認はこれまでどおり `bun run dev` の http://localhost:5173/tech-calendar/ 。
